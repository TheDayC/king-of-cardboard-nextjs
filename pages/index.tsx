import React, { useEffect } from 'react';
import Header from '../components/Header';

export const Home: React.FC = () => {
    return (
        <React.Fragment>
            <Header />
        </React.Fragment>
    );
};

export default Home;
