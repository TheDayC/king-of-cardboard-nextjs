export enum BreakTabs {
    Live,
    Archive,
}

export enum Status {
    Draft,
    Open,
    Live,
    Complete,
}

export enum Format {
    PickYourTeam,
    Random,
}

export enum Sport {
    Baseball,
    Basketball,
    Football,
    Formula1,
    Soccer,
    MMA,
    Wrestling,
}

export enum League {
    MLB,
    NBA,
    WNBA,
    NFL,
    Formula1,
    PremierLeague,
    Bundesliga,
    LaLiga,
    SerieA,
    UFC,
    WWE,
    AEW,
}
