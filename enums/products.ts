export enum Category {
    Sports,
    TCG,
    Other,
}

export enum Configuration {
    Sealed,
    Singles,
    Packs,
    Other,
}

export enum Interest {
    Baseball,
    Basketball,
    Football,
    Soccer,
    UFC,
    Wrestling,
    Pokemon,
    Other,
}
