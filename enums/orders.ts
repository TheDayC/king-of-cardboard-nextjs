export enum Status {
    Pending,
    Placed,
    Approved,
    Fulfilled,
    Cancelled,
}

export enum Payment {
    Unpaid,
    Authorised,
    Paid,
    Refunded,
}

export enum Fulfillment {
    Unfulfilled,
    InProgress,
    Fulfilled,
}
